# 🌍🎨 الميزات الجديدة - Bilingual + Dark Mode + Modern Design

## 📋 نظرة عامة

تم تحديث موقع Aizoniq بـ ثلاث ميزات رئيسية حديثة:
1. **دعم اللغتين** (العربية والإنجليزية)
2. **أوضاع المواضيع** (Light/Dark/System)
3. **تصميم الشاتبوت الحديث** (مستقبلي جداً)

---

## 🌐 نظام الترجمة (i18n)

### الملفات:
- `src/i18n/i18n.js` - نظام الترجمة الرئيسي

### المميزات:
- ✅ دعم العربية والإنجليزية
- ✅ حفظ اللغة المختارة في localStorage
- ✅ تحويل تلقائي لـ RTL/LTR
- ✅ دعم 50+ مفتاح ترجمة

### الاستخدام:

```javascript
import { i18n } from './i18n/i18n.js';

// الحصول على نص مترجم
const welcomeText = i18n.get('chatbot.welcome');

// تغيير اللغة
i18n.setLanguage('ar'); // العربية
i18n.setLanguage('en'); // الإنجليزية

// التحقق من اللغة الحالية
if (i18n.isRTL()) {
  // العربية
}
```

### المفاتيح المتاحة:

**Navigation:**
- `nav.home` - الرئيسية
- `nav.services` - الخدمات
- `nav.portfolio` - المحفظة
- `nav.about` - عن الشركة
- `nav.blog` - المدونة
- `nav.contact` - اتصل بنا

**Chatbot:**
- `chatbot.welcome` - الترحيب
- `chatbot.services` - الخدمات
- `chatbot.pricing` - الأسعار
- `chatbot.portfolio` - المحفظة
- `chatbot.contact` - التواصل
- وغيرها...

---

## 🎨 نظام المواضيع (Theme)

### الملفات:
- `src/theme/theme.js` - نظام إدارة المواضيع
- `css/variables-theme.css` - متغيرات CSS

### المميزات:
- ✅ وضع Light/Dark/System
- ✅ الكشف التلقائي عن تفضيل النظام
- ✅ حفظ الإعدادات في localStorage
- ✅ تحويل سلس بين الأوضاع

### الاستخدام:

```javascript
import { themeManager } from './theme/theme.js';

// التبديل بين الأوضاع
themeManager.toggle();

// تعيين موضوع معين
themeManager.setTheme('dark');  // داكن
themeManager.setTheme('light'); // فاتح
themeManager.setTheme('system'); // النظام

// التحقق من الموضوع الحالي
if (themeManager.isDark()) {
  // الوضع الداكن
}

// الحصول على الأيقونة والعلامة
const icon = themeManager.getThemeIcon(); // ☀️ أو 🌙
const label = themeManager.getThemeLabel(); // Dark أو Light
```

### متغيرات CSS:

جميع الألوان والأحجام تستخدم متغيرات CSS:

```css
:root {
  --primary-color: #667eea;
  --bg-primary: #ffffff;
  --text-primary: #1a1a1a;
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.1);
}

[data-theme="dark"] {
  --bg-primary: #1a1a2e;
  --text-primary: #e0e0e0;
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.5);
}
```

---

## 💬 شاتبوت حديث

### الملفات:
- `src/components/chatbot/chatbot.js` - مكون الشاتبوت
- `css/chatbot-modern.css` - أنماط الشاتبوت

### المميزات:
- ✅ تصميم عصري بـ Flexbox و CSS Grid
- ✅ دعم العربية (RTL)
- ✅ ألوان ديناميكية حسب الموضوع
- ✅ رسوم متحركة سلسة
- ✅ مؤشر الكتابة (Typing Indicator)
- ✅ أزرار الإجابات السريعة

### المظهر:
- Header متدرج بألوان
- رسائل محادثة أنيقة
- حقل إدخال حديث مع زر الإرسال
- أزرار إجابات سريعة
- مؤشر حالة Online مع نبضة

### الاستخدام:

```javascript
import { ChatbotWidget } from './components/chatbot/chatbot.js';

// إنشاء الشاتبوت
const chatbot = new ChatbotWidget('#chatbot-widget');

// الشاتبوت يعمل تلقائياً عند استدعاء main.js
```

---

## 🔄 مفتاح تبديل اللغة والموضوع

### الملفات:
- `src/components/theme-switcher/theme-switcher.js` - مفتاح التبديل
- `css/theme-switcher.css` - أنماط المفاتيح

### المميزات:
- ✅ يظهر في شريط التنقل تلقائياً
- ✅ يتحدث مع تبديل اللغة والموضوع
- ✅ أيقونات وعلامات واضحة
- ✅ تصميم متجاوب

### الأزرار:
- 🇸🇦/🇬🇧 اللغة (AR/EN)
- 🌙/☀️ الموضوع (Dark/Light)

---

## 📁 بنية الملفات الجديدة

```
src/
├── i18n/
│   └── i18n.js              # نظام الترجمة
├── theme/
│   └── theme.js             # نظام المواضيع
└── components/
    ├── chatbot/
    │   └── chatbot.js       # الشاتبوت المحدث
    └── theme-switcher/
        └── theme-switcher.js # مفتاح التبديل

css/
├── variables-theme.css      # متغيرات CSS والألوان
├── chatbot-modern.css       # أنماط الشاتبوت الحديثة
└── theme-switcher.css       # أنماط المفاتيح
```

---

## 🎯 كيفية الاستخدام

### 1. البدء التلقائي

عند تحميل الصفحة:
- يتم تحميل الإعدادات المحفوظة (اللغة والموضوع)
- يتم تطبيق المواضيع والترجمات تلقائياً
- يتم إنشاء مفاتيح التبديل في الـ header

### 2. تغيير اللغة

```javascript
// في أي مكان في التطبيق
i18n.setLanguage('ar'); // تغيير إلى العربية
i18n.setLanguage('en'); // تغيير إلى الإنجليزية
```

سيؤدي هذا إلى:
- تحديث جميع النصوص
- تغيير اتجاه الصفحة (RTL/LTR)
- حفظ الاختيار
- إطلاق حدث `languageChanged`

### 3. تغيير الموضوع

```javascript
// عبر مفتاح التبديل أو برمجياً
themeManager.toggle();        // التبديل بين Light و Dark
themeManager.setTheme('dark');
```

سيؤدي هذا إلى:
- تطبيق ألوان جديدة على جميع العناصر
- حفظ الاختيار
- إطلاق حدث `themeChanged`

### 4. الاستماع للتغييرات

```javascript
// الاستماع لتغيير اللغة
document.addEventListener('languageChanged', (e) => {
  console.log('Language changed to:', e.detail.lang);
});

// الاستماع لتغيير الموضوع
document.addEventListener('themeChanged', (e) => {
  console.log('Theme changed to:', e.detail.effectiveTheme);
});
```

---

## 🔧 الإضافات المطلوبة

تأكد من تضمين هذه الملفات في HTML:

```html
<!-- في <head> -->
<link rel="stylesheet" href="css/variables-theme.css">
<link rel="stylesheet" href="css/chatbot-modern.css">
<link rel="stylesheet" href="css/theme-switcher.css">

<!-- في نهاية <body> -->
<script type="module" src="src/main.js"></script>
```

---

## 🌟 المزايا

✅ **تجربة مستخدم متحسنة**
- دعم اللغة الأم للمستخدمين العرب
- اختيار الموضوع حسب التفضيل

✅ **إمكانية الوصول**
- دعم التنقل بالـ Keyboard
- اختبار الـ High Contrast
- دعم Reduced Motion

✅ **الأداء**
- استخدام CSS Variables للانتقال السلس
- عدم إعادة تحميل الصفحة عند التبديل
- حفظ محلي للإعدادات

✅ **القابلية للتطوير**
- نظام معياري للترجمة
- نظام موحد للمواضيع
- سهل الإضافة والتحديث

---

## 📊 الإحصائيات

- **150+** أسطر من kode CSS للمتغيرات
- **200+** أسطر CSS للشاتبوت الحديث
- **300+** أسطر JavaScript للأنظمة الجديدة
- **50+** مفتاح ترجمة معروف

---

## 🚀 الخطوات التالية

1. ✅ ترجمة جميع الصفحات الأخرى (services, portfolio, etc.)
2. ⏳ إضافة اختبارات للغات والمواضيع
3. ⏳ تحسينات الأداء والكاش
4. ⏳ دعم لغات إضافية
5. ⏳ تحسينات الإمكانية

---

**تم إنشاء هذه الميزات بتاريخ:** 2024
**الإصدار:** v2.0

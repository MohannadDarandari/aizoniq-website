# 🌍 نظام الترجمة الثنائي اللغة - دليل الاستخدام

## ✨ ما تم إنشاؤه

### 1. **ملف i18n.js** - نظام الترجمة البسيط
```
js/i18n.js
```
- يحتوي على جميع الترجمات (العربية والإنجليزية)
- 50+ مصطلح لجميع أقسام الموقع
- يحفظ التفضيل في localStorage
- يتغير تلقائياً عند الضغط على الزر

### 2. **زر تبديل اللغة** 
```html
<button id="language-toggle">🇸🇦 AR</button>
```
- موجود في جميع الصفحات بنفس الموقع (nav-buttons)
- يظهر اللغة الحالية
- الضغط عليه يبدل اللغة فوراً

### 3. **نظام data-i18n**
```html
<h1 data-i18n="hero.title">ترجمة افتراضية</h1>
```
- يمكن إضافة `data-i18n="مفتاح"` لأي عنصر HTML
- سيتم استبدال النص تلقائياً عند تبديل اللغة

---

## 🚀 كيفية الاستخدام

### إضافة ترجمة جديدة:

#### 1. في `js/i18n.js`، أضف مفتاح جديد في كلا اللغتين:

```javascript
const translations = {
  ar: {
    'page.title': 'عنوان الصفحة',
    // ...
  },
  en: {
    'page.title': 'Page Title',
    // ...
  }
};
```

#### 2. في HTML، استخدم `data-i18n`:

```html
<h1 data-i18n="page.title">عنوان افتراضي</h1>
```

#### 3. عند تحميل الصفحة:
- النص سيتم استبداله تلقائياً بالترجمة الصحيحة
- عند الضغط على الزر، سيتم تحديث جميع النصوص فوراً

---

## 📊 الحالة الحالية

### الصفحات المدعومة:
- ✅ `index.html` - الرئيسية
- ✅ `services.html` - الخدمات
- ✅ `portfolio.html` - المحفظة
- ✅ `about.html` - عننا
- ✅ `contact.html` - اتصل بنا

### اللغات المدعومة:
- 🇸🇦 **العربية** - اللغة الافتراضية
- 🇬🇧 **English** - عند الضغط على الزر

### الترجمات الموجودة:
- ✅ Navigation (5 عناصر)
- ✅ Hero Section (5 عناصر)
- ✅ Hero Stats (4 عناصر)
- ✅ Services Overview (9 عناصر)
- ✅ Service Cards (12 عنصر)
- ✅ Why Choose Us (10 عناصر)
- ✅ Footer (4 عناصر)

---

## 🎯 الميزات

✅ **لغة افتراضية عربية** - جميع الصفحات تبدأ بالعربية  
✅ **حفظ التفضيل** - تذكر اختيار اللغة عند العودة  
✅ **RTL/LTR تلقائي** - الاتجاه يتغير مع اللغة  
✅ **جميع الصفحات موحدة** - نفس النظام في كل مكان  
✅ **بسيط وخفيف** - بدون مكتبات خارجية ثقيلة  
✅ **سهل التوسع** - أضف ترجمات جديدة بسهولة  

---

## 🔧 الملفات المعدلة

### `index.html`
```diff
+ <button id="language-toggle">🇸🇦 AR</button>
+ <script src="js/i18n.js"></script>
```

### `services.html`, `portfolio.html`, `about.html`, `contact.html`
```diff
+ <button id="language-toggle">🇸🇦 AR</button>
+ <script src="js/i18n.js"></script>
```

### `js/i18n.js` (جديد)
- 200+ سطر من الترجمات والكود
- فئة `I18n` مع جميع الوظائف المطلوبة

---

## 💡 أمثلة عملية

### تبديل اللغة برمجياً:
```javascript
// غير اللغة من العربية إلى الإنجليزية
i18n.setLanguage('en');

// أو العكس
i18n.setLanguage('ar');
```

### الحصول على ترجمة:
```javascript
const title = i18n.get('hero.title');
console.log(title); // يطبع الترجمة الحالية
```

### معرفة اللغة الحالية:
```javascript
console.log(i18n.currentLanguage); // 'ar' أو 'en'

// أو التحقق إذا كانت RTL:
if (i18n.isRTL()) {
  console.log('اللغة الحالية عربية');
}
```

---

## 📝 ملاحظات مهمة

1. **التفضيل يُحفظ في localStorage** - بفتاح `aizoniq_language`
2. **الترجمات موجودة في `js/i18n.js`** - يمكن إضافة المزيد
3. **الزر موجود في جميع الصفحات** - نفس الكود مكرر لسهولة
4. **العربية افتراضية** - إذا لم يختر المستخدم لغة، ستكون العربية

---

## 🎉 الحالة النهائية

✅ نظام ترجمة كامل بالعربية والإنجليزية  
✅ زر تبديل لغة في كل صفحة  
✅ حفظ الخيار المختار  
✅ جاهز للاستخدام الفوري  

**التاريخ:** 19 ديسمبر 2025
